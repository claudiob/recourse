<%# The rows are worked out rather than read off a table; the table drawing them is the
    gem's, and so are the paging under it and the chrome around it. %>
<% content_for :title, 'Weeks' %>

<%= render 'table', recourses: @resources, pagy: @pagy %>
