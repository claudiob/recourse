# What both halves of a person's steps listing answer: the page that draws it and the
# write a drop lands on. A step holds a place among its team's steps and another among
# the steps of whoever is to do them, and which of the two a page is in is the page's
# answer rather than the model's — a model keeps one column, and this is the listing
# that reads the other.
module Ranking
  extend ActiveSupport::Concern

private

  def recourse_position = 'ranking'
end
